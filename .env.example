# Billetterie Blessing Event — variables d'environnement

# Firebase (optionnel — localStorage par défaut)
VITE_USE_FIRESTORE=false

# Clés Firebase (déjà dans src/firebase.ts en fallback)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=

# Stripe (production)
# VITE_STRIPE_PUBLIC_KEY=pk_live_...
